---
crew:
  - name: naomi
    groups: wheel,brains
    shell: /bin/bash
    expires: -1

  - name: amos
    groups: muscle
    shell: /sbin/nologin
    expires: "{{ today_plus_60 }}"

  - name: alex
    groups: brains
    shell: /bin/sh
    expires: "{{ today_plus_60 }}"

  - name: bobbie
    groups: muscle
    shell: /bin/bash
    expires: "{{ today_plus_60 }}"
